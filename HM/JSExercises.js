// 1 - Write a function that takes an integer minutes and converts it to seconds.
function minutesToSecond(minuts) {
    return minuts * 60;
}

// 2 - Create a function that takes a number as an argument, increments the number by +1 and returns the result.
function incrementByOne(num) {
    return num + 1;
}

// 3 - Write a function that takes the base and height of a triangle and return its area.
function calculateArea() {
    let base = 30;
    let height = 30;
    let s = (base + height / 2);
    return area = Math.sqrt(s * ((s - base) * (s - height)));
}

// 4 - Create a function that takes the age in years and returns the age in days.
function ageToDays(age) {
    return age / (1000 * 60 * 60 * 24);
}
// console.log(ageToDays(27));

// 5 -Create a function that takes an array containing only numbers and return the first element.
function getFirstNumber(num) {
    return num[0];
}

//  6 - Write a function that converts hours into seconds.
function hoursToSeconds(params) {
    let seconds = (hours * 60) * 60;
    return seconds;
}

// 7 - Create a function that takes length and width and finds the perimeter of a rectangle.
function name(params) {

}

// 8 - Given two numbers, return true if the sum of both numbers is less than 100. Otherwise return false.
function lessThen(num1, num2) {
    if (num1 + num2 < 100) {
        return true;
    }

    return false;
}
// console.log(lessThen(40, 30));

// 9 - Write a function that takes two integers (hours, minutes), converts them to seconds, and adds them.
function addHoursMinutes(hours, minutes) {
    return hours * 3600 + minutes * 60
}
// console.log(addHoursMinutes(2, 43));

// 10 - Create a function that takes two arguments
function findSum(a, b) {

    if (a === 10 || b === 10 || a + b == 10) {
        return true;
    }
    return false;
}
// console.log(findSum(3,6));

// // 11 - Given two arguments, return an array which contains these two arguments.
function returnArr(first, second) {
    return [first, second];
}
// console.log(returnArr(2,6))

// 12 - Create a function that returns true if an integer is evenly divisible by 5, and false otherwise.
// function isDivisibleBy(int) {
//     if () {
//         return true;
//     }
//     return false;
// }
// console.log(isDivisibleBy(234))

// // 13 - Some basic arithmetic operators are +, -, *, /, and %. In this challenge you will be given three parameters, num1, num2, and an operator. Use the operator on number 1 and 2.
// function name(params) {

// }

// // 14 -
// function name(params) {

// }

// // 15 -
// function name(params) {

// }
